# Direct dependencies for SteadyPlan
# Run `.venv/bin/pip-compile --generate-hashes` to lock all transitive deps

# Web framework + auth
Flask==3.1.3
Flask-Login>=0.6.3
Flask-WTF>=1.2.2

# API security & rate limiting
Flask-Limiter>=4.1.1
cryptography>=49.0.0

# Price lookups (redundant: Yahoo + Twelve Data)
yfinance>=1.5.1

# Scheduled jobs (price refresh, backups)
APScheduler>=3.11.3
pytz>=2026.2

# Excel export (scenario estimates, budget, performance)
openpyxl>=3.1.5

# Production WSGI server
gunicorn>=26.0.0
